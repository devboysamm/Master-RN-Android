// Single source of truth for the backend the app talks to.
// No secrets live in the app, and no old-project values appear here.
// Every network call must target this base URL (see API_REFERENCE.md, Section 1).
export const API_BASE_URL = 'https://api.masterreactnative.me';
